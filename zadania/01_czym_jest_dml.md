# Zadania 01 - czym jest DML

## Zadanie 1

Własnymi słowami napisz, czym różni się `SELECT` od `INSERT`, `UPDATE` i `DELETE`.

## Zadanie 2

Podejrzyj wszystkie dane z tabeli `course.customers`.

## Zadanie 3

Podejrzyj wszystkie dane z tabeli `course.products`.

## Zadanie 4

Podejrzyj wszystkie dane z tabeli `course.orders`.

## Zadanie 5

Podejrzyj wszystkie dane z tabeli `course.order_items`.

## Zadanie 6

Napisz `SELECT`, który pokazuje klienta o `customer_id = 1`.

## Zadanie 7

Napisz `SELECT`, który pokazuje produkt o `product_id = 101`.

## Zadanie 8

Napisz `SELECT`, który pokazuje zamówienie o `order_id = 1001`.

## Zadanie 9

Napisz `SELECT`, który pokazuje pozycję zamówienia o `order_item_id = 1`.

## Zadanie 10

Napisz jednym zdaniem, dlaczego przed `UPDATE` albo `DELETE` warto najpierw wykonać `SELECT`.

## Zadanie 11

Napisz jednym zdaniem, co może się stać, jeśli uruchomisz `UPDATE` bez `WHERE`.

## Zadanie 12

Napisz jednym zdaniem, co może się stać, jeśli uruchomisz `DELETE` bez `WHERE`.
